
#include "Channel.hpp"
#include "parsingUtils.hpp"


Channel::Channel(std::string name, std::string topic, std::string pass, int size_limit)
    : name(n), topic(t), password(p), size_limit(s), mode(NO_FLAG) {
	}

